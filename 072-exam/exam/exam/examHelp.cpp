#include <iostream>
#include <iomanip>
#include <string>

using namespace std;

int main()
{
    #include <iomanip>
    cout << fixed << setprecision(2) << 


    cout.setf(std::ios::fixed);
    cout.precision(2);

    #include <string>
    cin.ignore();
    getline(cin, text);

    #include <climits>
    INT_MAX
        INT_MIN

    #include <math.h>
        ceil(); // закръгляме нагоре
        floor(); // закръгляне нодалу
        abs();  // абсолютна стойност

    //достъп до цифрите на число
    while (currentNum != 0) {
        n = currentNum % 10;
        currentNum /= 10;
    }
    
    //работа с текст
    string name = "vlado";
    int numberLetters = name.length();  // vlado = 5 букви
    int numberLetters = name.size();

    char letter = text[i];  // присвояваме на символа стойтостта на i-тата буква

    // часове и минути
    string(minutes < 10, '0');
 
    //тернарен оператор
    (true/false ? "anwer true" : "answer false")

    #include <cmath>
    M_PI

}
